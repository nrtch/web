#-----------------------------------------------------------------------
version: '3.7'
#-----------------------------------------------------------------------
services:
#-----------------------------------------------------------------------
  nexx_me_front:
    image: ${repoUser}/${repoName}:${appName}
    command: PORT=8080 yarn start
    deploy:
      mode: replicated
      replicas: 2
      placement:
        constraints:
          - node.hostname==${deployNode}
      restart_policy:
        condition: any
    networks:
      - appnet
      - webnet
#-----------------------------------------------------------------------
networks:
  appnet:
    external: true
    name: CICD_appnet
  webnet:
    external: true
    name: CICD_webnet
